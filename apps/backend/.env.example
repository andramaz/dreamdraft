# API
PORT=3000
# Comma-separated list of allowed frontend origins (CORS)
CORS_ORIGINS=http://localhost:5173

# Separate database for this app on the existing PostgreSQL instance.
# Create it first:  CREATE DATABASE dreamdraft;
DATABASE_URL="postgresql://USER:PASSWORD@localhost:5432/dreamdraft?schema=public"
